# Argos launch planning — September 11, 2026

Research only. No approvals, signatures, transactions or launch submissions performed.

## Owner-selected draft

Argos Bot / ARGOS; 300 USDC developer-buy input; allocation 100% creator, 0% burn, 0% dividends, 0% liquidity. Use the owner-supplied X image URL and current Argos website/X/Telegram links recorded in `argos-launch-draft.json`. Description: "Your gateway to Arc chain." Buy/sell tax rates remain unspecified. Explicit instruction: do not launch now. No simulation or signing has occurred for this draft.

## Wallet and live checks

Requested creator: 0x7d381D70e3Cc6532Fd5546e5439bC3D5CeCD28DC (@0xTheOdysseus, identity supplied by owner). Existing CDP account lookup matched this address. No wallet was created or renamed. This is also the configured OTC fee recipient: coordinate reservations, nonce use and available funds with the wallet service.

At block 20334581: balance 447.1059108925 native Arc USDC; chain ID 5042; Portal 0xa5628a11c412596e1f63b75a2c0284f843c549d6; launch record word count 11; three registered tokens. Separate live reads found USDC approved as a quote, wallet allowance to this Portal zero, and LaunchConfig configFor(wallet) = (0, 0). Recheck before signing.

## Evidence

See onchain-review-2026-09-11.json for public transaction hashes, decoded direct launch inputs, deployed records, gas and metadata. Newest first:

- FROGFISH: successful transaction, 2,942,480 gas / 0.06326332 USDC. An intermediary at 0x9418657d2d66E8C6dF3c54D164a27E1f1dF6944c is the recorded creator. Its outer call does not match the Portal launch ABI; no developer-buy or allocation inference is made from that outer payload. Recorded taxes are 4% each and quote is USDC.
- Baby Argus: successful direct launch, 3,551,054 gas / 0.170450592 USDC. ARGUS quote asset, developer-buy input 2 ARGUS, 1% buy/sell taxes, 100% dividend allocation. This contradicts the bundle note that every launch uses USDC.
- ARCBAE: successful direct launch, 3,121,529 gas / 0.137347276 USDC. USDC quote, 15 USDC developer-buy input, 1% buy/sell taxes. Allocation: creator 50%, burn 25%, dividends 0%, liquidity 25%. Supply 1 billion; opening FDV 2,500 USDC and bond FDV 45,000 USDC. All three outer transactions carry zero native value. Gas figures exclude separate setup/approval transactions and are not future quotes.

## Proposed operator workflow

1. Obtain final name, ticker, description, image, buy/sell tax, allocation and developer-buy budget. Use current Argos website/X/TG links. Do not infer token economics from the bot service fee.
2. Use the refreshed version-2 Portal ABI, not the saved old payable Portal or legacy launch scripts. Keep public website/X/TG launch commands disabled.
3. Add a dedicated operator preparation path using the existing CDP account, wallet reservation and nonce coordination. Normal transaction gas cap is 1 million; allow an independently reviewed, simulated launch budget rather than globally raising it.
4. Generate a random token salt; search a separate hook salt satisfying the required v4 hook-address flags, and verify predicted token/splitter/hook with Portal getters. Refresh implementation pointers before signing.
5. If dividends are chosen, prepare and verify the current LaunchConfig setup. If a developer buy is chosen, approve only its exact USDC amount to the current Portal and verify allowance.
6. Simulate the exact launch from the creator address with zero native value. Verify quote, taxes, allocations, metadata, predicted addresses, gas and available funds. Present the complete payload and spend limit for final approval.
7. Submit through CDP with durable idempotency and receipt recovery. Confirm TokenCreated and the deployed creator, quote, hook, splitter, locker, pool ID, taxes and actual token delivery. Capture receipts and gas; add the token to the index and perform read-only trading quotes.

## Sources and constraints

https://arguspad.io/docs — current mainnet lifecycle, taxes, LP and developer-buy behavior.
https://arguspad.io/argus-v4.json — version 2, generated September 11; snapshot saved beside this plan. Current launch is nonpayable and developer buying is funded by ERC-20 approval. Recorded per-token state takes precedence over general notes.
https://arguspad.io/create — image requirement (square, at least 400 px), 280-character description field, creator payment wallet is the signing wallet. Rates and creator destination are permanent. A new token can use existing tickers; confirm intended ticker/address rather than assuming uniqueness.

Outstanding: final economics/artwork approval, operator launch implementation, exact simulation and final execution authorization. No launch has been simulated with user-selected parameters yet.
